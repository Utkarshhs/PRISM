/**
 * Re-export: live pipeline demo UI lives in DemoCenter.jsx (Stage 7 uses Socket.io).
 * This file exists so imports match the documented component name.
 */
export { default } from './DemoCenter';
